import sentiment_analysis
import reddit
import instagram

def scrapeSites(parameters):
    text = []
    text.append(reddit.scraper(location, topic, limit))
    text.append(instagram.scraper(location, topic, limit))


def analyse(text):
    analysis.analyse(text)
